openapi: 3.0.3
info:
  title: Holder API
  version: 0.1.0
servers:
  - url: /
  - url: http://127.0.0.1:11499
security:
  - bearerAuth: []
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: token
  schemas:
    Error:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string
    ErrorResponse:
      type: object
      required: [ok, error]
      properties:
        ok:
          type: boolean
          enum: [false]
        error:
          $ref: "#/components/schemas/Error"
    HealthData:
      type: object
      required: [db_ok, uptime_ms, api_version, server_version, pid, crypt_filter]
      properties:
        db_ok:
          type: boolean
        uptime_ms:
          type: integer
          format: int64
        api_version:
          type: string
        server_version:
          type: string
        pid:
          type: integer
        crypt_filter:
          type: object
          required: [path, exists, executable]
          properties:
            path:
              type: string
            exists:
              type: boolean
            executable:
              type: boolean
    HealthResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/HealthData"
    LocalModel:
      type: object
      required: [name]
      properties:
        name:
          type: string
        digest:
          type: string
        size:
          type: integer
          format: int64
        modified_at:
          type: string
    CasteInfo:
      type: object
      required: [name]
      properties:
        name:
          type: string
          enum: [mini, user, developer, workstation, rig]
        reason:
          type: string
          nullable: true
    CasteRecommendedModel:
      type: object
      required: [tag, required_caste, installed]
      properties:
        tag:
          type: string
        provider:
          type: string
          nullable: true
        engine:
          type: string
          nullable: true
        category:
          type: string
          nullable: true
        required_caste:
          type: string
          enum: [mini, user, developer, workstation, rig]
        installed:
          type: boolean
    RunnerCapabilitiesData:
      type: object
      required: [runner_available, last_checked, local_model_config, models, recommended_models, recommended_install]
      properties:
        runner_available:
          type: boolean
        spawn_attempted:
          type: boolean
        last_checked:
          type: integer
          format: int64
        version:
          type: string
        error:
          type: string
          nullable: true
        caste:
          allOf:
            - $ref: "#/components/schemas/CasteInfo"
          nullable: true
        local_model_config:
          $ref: "#/components/schemas/AiLocalModelConfigData"
        models:
          type: array
          items:
            $ref: "#/components/schemas/LocalModel"
        recommended_models:
          type: array
          items:
            $ref: "#/components/schemas/CasteRecommendedModel"
        recommended_install:
          type: array
          items:
            $ref: "#/components/schemas/CasteRecommendedModel"
    RunnerCapabilitiesResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/RunnerCapabilitiesData"
    AiLocalModelConfigData:
      type: object
      required: [fast_model, strong_model, deep_model, updated_at]
      properties:
        fast_model:
          type: string
          nullable: true
        strong_model:
          type: string
          nullable: true
        deep_model:
          type: string
          nullable: true
        updated_at:
          type: integer
          format: int64
          nullable: true
    AiLocalModelConfigResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/AiLocalModelConfigData"
    AiLocalModelConfigUpdateRequest:
      type: object
      properties:
        fast_model:
          type: string
          nullable: true
        strong_model:
          type: string
          nullable: true
        deep_model:
          type: string
          nullable: true
        updated_at:
          type: integer
          format: int64
          nullable: true
    RunnerPullProgress:
      type: object
      required: [completed, total, percent, stage]
      properties:
        completed:
          type: integer
          format: int64
        total:
          type: integer
          format: int64
        percent:
          type: number
          format: double
        stage:
          type: string
    RunnerPullJob:
      type: object
      required: [job_id, model, status, updated_at, progress]
      properties:
        job_id:
          type: string
        model:
          type: string
        status:
          type: string
        updated_at:
          type: integer
          format: int64
        error:
          type: string
          nullable: true
        progress:
          $ref: "#/components/schemas/RunnerPullProgress"
    RunnerPullStartResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [job_id, model, status]
          properties:
            job_id:
              type: string
            model:
              type: string
            status:
              type: string
    RunnerPullStatusResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/RunnerPullJob"
    AiStatusPullJob:
      allOf:
        - $ref: "#/components/schemas/RunnerPullJob"
        - type: object
          required: [active]
          properties:
            active:
              type: boolean
    AiProviderCredential:
      type: object
      required: [provider, configured, api_key_preview, created_at, updated_at]
      properties:
        provider:
          type: string
        configured:
          type: boolean
        api_key_preview:
          type: string
        created_at:
          type: integer
          format: int64
        updated_at:
          type: integer
          format: int64
    AiProviderCredentialsListResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [providers]
          properties:
            providers:
              type: array
              items:
                $ref: "#/components/schemas/AiProviderCredential"
    AiProviderCredentialUpsertRequest:
      type: object
      required: [provider, api_key]
      properties:
        provider:
          type: string
        api_key:
          type: string
        updated_at:
          type: integer
          format: int64
          nullable: true
    AiProviderCredentialUpsertResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/AiProviderCredential"
    AiProviderCredentialDeleteResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [provider]
          properties:
            provider:
              type: string
    AiProviderCatalogProvider:
      type: object
      required: [id, display_name, enabled, configured, api, auth, models]
      properties:
        id:
          type: string
        display_name:
          type: string
        enabled:
          type: boolean
        configured:
          type: boolean
        api:
          type: object
          additionalProperties: true
          nullable: true
        auth:
          type: object
          additionalProperties: true
          nullable: true
        models:
          type: array
          items:
            type: object
            additionalProperties: true
        setup_url:
          type: string
          nullable: true
        docs_url:
          type: string
          nullable: true
        api_key_label:
          type: string
          nullable: true
        api_key_hint:
          type: string
          nullable: true
    AiProviderCatalogResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [providers]
          properties:
            default_provider:
              type: string
              nullable: true
            providers:
              type: array
              items:
                $ref: "#/components/schemas/AiProviderCatalogProvider"
    AiStatusData:
      type: object
      required: [checked_at, runner_available, runner_last_checked, active_runs, active_pull_jobs, pulls, cloud, cloud_configured_providers]
      properties:
        checked_at:
          type: integer
          format: int64
        runner_available:
          type: boolean
        runner_last_checked:
          type: integer
          format: int64
        runner_version:
          type: string
          nullable: true
        runner_error:
          type: string
          nullable: true
        active_runs:
          type: integer
          format: int64
        active_pull_jobs:
          type: integer
          format: int64
        pulls:
          type: array
          items:
            $ref: "#/components/schemas/AiStatusPullJob"
        cloud:
          type: array
          items:
            $ref: "#/components/schemas/AiProviderCredential"
        cloud_configured_providers:
          type: integer
          format: int64
    AiStatusResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/AiStatusData"
    AiRunCreateRequest:
      type: object
      required: [prompt]
      properties:
        prompt:
          type: string
        mode:
          type: string
          description: auto or model
        model:
          type: string
          description: Optional explicit model id; interpreted for local or cloud selected provider.
        provider:
          type: string
          description: Optional cloud provider id when local runner is unavailable.
        project_id:
          type: string
        thread_id:
          type: string
        context:
          type: object
          additionalProperties: true
    AiRunItem:
      type: object
      required: [run_id, mode, prompt, status, created_at, updated_at]
      properties:
        run_id:
          type: string
        project_id:
          type: string
          nullable: true
        thread_id:
          type: string
          nullable: true
        message_id:
          type: string
          nullable: true
        mode:
          type: string
        prompt:
          type: string
        context_json:
          type: string
          nullable: true
        router_model:
          type: string
          nullable: true
        ranked_json:
          type: string
          nullable: true
        policy_trace_json:
          type: string
          nullable: true
        policy_trace:
          type: object
          nullable: true
          additionalProperties: true
        chosen_model:
          type: string
          nullable: true
        status:
          type: string
        error:
          type: string
          nullable: true
        created_at:
          type: integer
          format: int64
        updated_at:
          type: integer
          format: int64
    AiRunListResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: array
          items:
            $ref: "#/components/schemas/AiRunItem"
    AiRunGetResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/AiRunItem"
    RebuildRequest:
      type: object
      required: [project_id]
      properties:
        project_id:
          type: string
    RebuildData:
      type: object
      required: [project_id, cards, ai_messages, ai_threads, links]
      properties:
        project_id:
          type: string
        cards:
          type: integer
        ai_messages:
          type: integer
        ai_threads:
          type: integer
        links:
          type: integer
    RebuildResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/RebuildData"
    ReindexData:
      type: object
      required: [message]
      properties:
        message:
          type: string
    ReindexResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/ReindexData"
    CardCreateRequest:
      type: object
      required: [project_id, title, content]
      properties:
        card_id:
          type: string
          description: Optional; server generates if omitted.
        project_id:
          type: string
        title:
          type: string
        content:
          type: string
        created_at:
          type: integer
          format: int64
          description: Optional; server uses current time if omitted or 0.
        updated_at:
          type: integer
          format: int64
          description: Optional; server uses created_at if omitted or 0.
        parent_card_id:
          type: string
          nullable: true
        sort_key:
          type: number
          format: double
        rel_path:
          type: string
          nullable: true
    CardCreateResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [card_id, rel_path]
          properties:
            card_id:
              type: string
            rel_path:
              type: string
    CardUpdateRequest:
      type: object
      required: [updated_at]
      properties:
        content:
          type: string
        updated_at:
          type: integer
          format: int64
        title:
          type: string
          nullable: true
        parent_card_id:
          type: string
          nullable: true
        sort_key:
          type: number
          format: double
    CardData:
      type: object
      required:
        [card_id, project_id, title, rel_path, sort_key, created_at, updated_at, content]
      properties:
        card_id:
          type: string
        project_id:
          type: string
        title:
          type: string
        rel_path:
          type: string
        sort_key:
          type: number
          format: double
        created_at:
          type: integer
          format: int64
        updated_at:
          type: integer
          format: int64
        parent_card_id:
          type: string
          nullable: true
        deleted_at:
          type: integer
          format: int64
          nullable: true
        content:
          type: string
    CardGetResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/CardData"
    CardListItem:
      type: object
      required:
        [card_id, project_id, title, rel_path, sort_key, created_at, updated_at]
      properties:
        card_id:
          type: string
        project_id:
          type: string
        title:
          type: string
        rel_path:
          type: string
        sort_key:
          type: number
          format: double
        created_at:
          type: integer
          format: int64
        updated_at:
          type: integer
          format: int64
        parent_card_id:
          type: string
          nullable: true
        deleted_at:
          type: integer
          format: int64
          nullable: true
        child_count:
          type: integer
          minimum: 0
          description: Present when count=true.
    CardListResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: array
          items:
            $ref: "#/components/schemas/CardListItem"
    CardPatchResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [card_id]
          properties:
            card_id:
              type: string
    CardMoveIntent:
      type: string
      enum: [into, before, after, left, right, to_start, to_end, up_level]
    CardMoveRequest:
      type: object
      required: [project_id, intent]
      properties:
        project_id:
          type: string
        intent:
          $ref: "#/components/schemas/CardMoveIntent"
        target_card_id:
          type: string
          nullable: true
          description: Required for intents into/before/after.
        parent_card_id:
          type: string
          nullable: true
          description: Optional parent scope for intents to_start/to_end/left/right. If omitted, source card's current parent scope is used.
        if_revision:
          type: integer
          format: int64
          nullable: true
          description: Optional optimistic-concurrency revision guard.
    CardMoveResult:
      type: object
      required: [card_id, parent_card_id, sort_key, revision]
      properties:
        card_id:
          type: string
        parent_card_id:
          type: string
          nullable: true
        sort_key:
          type: number
          format: double
        revision:
          type: integer
          format: int64
        moved_into_title:
          type: string
          nullable: true
    CardMoveResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/CardMoveResult"
    CardContextCard:
      allOf:
        - $ref: "#/components/schemas/CardListItem"
        - type: object
          properties:
            child_count:
              type: integer
    CardContextBreadcrumb:
      type: object
      required: [type, title]
      properties:
        type:
          type: string
          enum: [project, card]
        title:
          type: string
        project_id:
          type: string
          nullable: true
        card_id:
          type: string
          nullable: true
    CardContextData:
      type: object
      required: [project, current_parent_card_id, breadcrumbs, cards]
      properties:
        project:
          type: object
          required: [project_id, name]
          properties:
            project_id:
              type: string
            name:
              type: string
        current_parent_card_id:
          type: string
          nullable: true
        breadcrumbs:
          type: array
          items:
            $ref: "#/components/schemas/CardContextBreadcrumb"
        cards:
          type: array
          items:
            $ref: "#/components/schemas/CardContextCard"
    CardContextResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/CardContextData"
    CardLink:
      type: object
      required: [from_card_id, to_card_id, to_type, kind, created_at]
      properties:
        from_card_id:
          type: string
        to_card_id:
          type: string
        to_type:
          type: string
          description: Target type (card, ai_message, ai_thread, resource).
        kind:
          type: string
        label:
          type: string
          nullable: true
        created_at:
          type: integer
          format: int64
    CardLinkCreateRequest:
      type: object
      required: [to_card_id]
      properties:
        to_card_id:
          type: string
        to_type:
          type: string
          description: Optional; defaults to "card".
        kind:
          type: string
          description: Optional; defaults to "ref".
        label:
          type: string
          nullable: true
        created_at:
          type: integer
          format: int64
          description: Optional; server uses current time if omitted or 0.
    CardLinkCreateResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/CardLink"
    CardLinkListResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: array
          items:
            $ref: "#/components/schemas/CardLink"
    CardLinkDeleteRequest:
      type: object
      properties:
        to_card_id:
          type: string
        to_type:
          type: string
        kind:
          type: string
    Milestone:
      type: object
      required: [milestone_id, card_id, start_at, all_day, created_at, updated_at]
      properties:
        milestone_id:
          type: string
        card_id:
          type: string
        start_at:
          type: integer
          format: int64
        end_at:
          type: integer
          format: int64
          nullable: true
        all_day:
          type: boolean
        kind:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        created_at:
          type: integer
          format: int64
        updated_at:
          type: integer
          format: int64
        card_title:
          type: string
          description: Included in project calendar responses.
    MilestoneCreateRequest:
      type: object
      required: [start_at]
      properties:
        start_at:
          type: integer
          format: int64
        end_at:
          type: integer
          format: int64
          nullable: true
        all_day:
          type: boolean
          default: false
        kind:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
    MilestoneResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/Milestone"
    MilestoneListResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: array
          items:
            $ref: "#/components/schemas/Milestone"
    CalendarCardActivity:
      type: object
      required: [card_id, title, created_at, updated_at]
      properties:
        card_id:
          type: string
        title:
          type: string
        created_at:
          type: integer
          format: int64
        updated_at:
          type: integer
          format: int64
    ProjectCalendarResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [project_id, from, to, milestones, created_cards, updated_cards]
          properties:
            project_id:
              type: string
            from:
              type: integer
              format: int64
            to:
              type: integer
              format: int64
            milestones:
              type: array
              items:
                $ref: "#/components/schemas/Milestone"
            created_cards:
              type: array
              items:
                $ref: "#/components/schemas/CalendarCardActivity"
            updated_cards:
              type: array
              items:
                $ref: "#/components/schemas/CalendarCardActivity"
    SearchCardItem:
      type: object
      required: [card_id, title, updated_at, created_at, snippet, rank]
      properties:
        card_id:
          type: string
        title:
          type: string
        updated_at:
          type: integer
          format: int64
        created_at:
          type: integer
          format: int64
        snippet:
          type: string
        rank:
          type: number
          format: double
    SearchMessageItem:
      type: object
      required: [message_id, created_at, snippet, rank]
      properties:
        message_id:
          type: string
        created_at:
          type: integer
          format: int64
        snippet:
          type: string
        rank:
          type: number
          format: double
    AiMessage:
      type: object
      required: [message_id, thread_id, role, source, content, created_at]
      properties:
        message_id:
          type: string
        thread_id:
          type: string
        role:
          type: string
        source:
          type: string
        provider:
          type: string
          nullable: true
        model:
          type: string
          nullable: true
        content:
          type: string
        created_at:
          type: integer
          format: int64
        deleted_at:
          type: integer
          format: int64
          nullable: true
        prompt_hash:
          type: string
          nullable: true
        meta_json:
          type: string
          nullable: true
    AiMessageCreateRequest:
      type: object
      required: [thread_id, role, source, content]
      properties:
        message_id:
          type: string
          description: Optional; server generates if omitted.
        thread_id:
          type: string
        role:
          type: string
        source:
          type: string
        provider:
          type: string
          nullable: true
        model:
          type: string
          nullable: true
        content:
          type: string
        created_at:
          type: integer
          format: int64
          description: Optional; server uses current time if omitted or 0.
        prompt_hash:
          type: string
          nullable: true
        meta_json:
          type: string
          nullable: true
    AiMessageCreateResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [message_id]
          properties:
            message_id:
              type: string
    AiMessageCaptureRequest:
      type: object
      required: [project_id, prompt, response]
      properties:
        project_id:
          type: string
        thread_id:
          type: string
          nullable: true
        prompt:
          type: string
        response:
          type: string
        source:
          type: string
          nullable: true
        provider:
          type: string
          nullable: true
        model:
          type: string
          nullable: true
        url:
          type: string
          nullable: true
        context:
          type: object
          additionalProperties: true
          nullable: true
        created_at:
          type: integer
          format: int64
          nullable: true
    AiMessageCaptureResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [thread_id, user_message_id, assistant_message_id]
          properties:
            thread_id:
              type: string
            user_message_id:
              type: string
            assistant_message_id:
              type: string
    AiMessageUpdateRequest:
      type: object
      properties:
        role:
          type: string
          nullable: true
        source:
          type: string
          nullable: true
        provider:
          type: string
          nullable: true
        model:
          type: string
          nullable: true
        content:
          type: string
          nullable: true
        created_at:
          type: integer
          format: int64
        prompt_hash:
          type: string
          nullable: true
        meta_json:
          type: string
          nullable: true
    AiMessageUpdateResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [message_id]
          properties:
            message_id:
              type: string
    AiThread:
      type: object
      required: [thread_id, project_id, title, created_at, updated_at]
      properties:
        thread_id:
          type: string
        project_id:
          type: string
        card_id:
          type: string
          nullable: true
        title:
          type: string
        created_at:
          type: integer
          format: int64
        updated_at:
          type: integer
          format: int64
    AiThreadCreateRequest:
      type: object
      required: [project_id, title]
      properties:
        thread_id:
          type: string
          description: Optional; server generates if omitted.
        project_id:
          type: string
        title:
          type: string
        card_id:
          type: string
          nullable: true
        created_at:
          type: integer
          format: int64
          description: Optional; server uses current time if omitted or 0.
        updated_at:
          type: integer
          format: int64
          description: Optional; server uses created_at if omitted or 0.
    AiThreadCreateResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [thread_id]
          properties:
            thread_id:
              type: string
    AiThreadUpdateRequest:
      type: object
      required: [updated_at]
      properties:
        title:
          type: string
          nullable: true
        card_id:
          type: string
          nullable: true
        updated_at:
          type: integer
          format: int64
    AiThreadListResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: array
          items:
            $ref: "#/components/schemas/AiThread"
    AiThreadGetResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/AiThread"
    AiThreadPatchResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [thread_id]
          properties:
            thread_id:
              type: string
    AiMessageListResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: array
          items:
            $ref: "#/components/schemas/AiMessage"
    AiMessageGetResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/AiMessage"
    Project:
      type: object
      required: [project_id, name, root_path, privacy_mode, created_at, updated_at, sync]
      properties:
        project_id:
          type: string
        name:
          type: string
        root_path:
          type: string
        git_remote_url:
          type: string
          nullable: true
        git_provider:
          type: string
          nullable: true
        privacy_mode:
          type: string
          enum: [encrypted_git, plain]
        project_key_id:
          type: string
          nullable: true
        created_at:
          type: integer
          format: int64
        updated_at:
          type: integer
          format: int64
        card_count:
          type: integer
          minimum: 0
          description: Present when count=true.
        root_card_count:
          type: integer
          minimum: 0
          description: Present when count=true.
        sync:
          $ref: "#/components/schemas/ProjectSync"
    ProjectSync:
      type: object
      required:
        [
          uncommitted_changes_count,
          unpushed_commits_count,
          retry_count
        ]
      properties:
        last_commit_at:
          type: integer
          format: int64
          nullable: true
        last_push_at:
          type: integer
          format: int64
          nullable: true
        last_pull_at:
          type: integer
          format: int64
          nullable: true
        uncommitted_changes_count:
          type: integer
        unpushed_commits_count:
          type: integer
        last_push_status:
          type: string
          nullable: true
        last_pull_status:
          type: string
          nullable: true
        last_sync_error:
          type: string
          nullable: true
        last_sync_error_at:
          type: integer
          format: int64
          nullable: true
        retry_count:
          type: integer
        next_retry_at:
          type: integer
          format: int64
          nullable: true
        updated_at:
          type: integer
          format: int64
          nullable: true
    ProjectCreateRequest:
      type: object
      required: [name]
      properties:
        project_id:
          type: string
          description: Optional; server generates if omitted.
        name:
          type: string
        root_path:
          type: string
          description: Optional; server chooses a default if omitted.
        git_remote_url:
          type: string
          nullable: true
        git_provider:
          type: string
          nullable: true
        privacy_mode:
          type: string
          enum: [encrypted_git, plain]
          description: Optional; defaults to encrypted_git.
        project_key_id:
          type: string
          nullable: true
          description: Optional keyring identifier (metadata only).
        created_at:
          type: integer
          format: int64
          description: Optional; server uses current time if omitted or 0.
        updated_at:
          type: integer
          format: int64
          description: Optional; server uses created_at if omitted or 0.
    ProjectCreateResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [project_id]
          properties:
            project_id:
              type: string
    ProjectListResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: array
          items:
            $ref: "#/components/schemas/Project"
    ProjectGetResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/Project"
    ProjectUpdateRequest:
      type: object
      required: [updated_at]
      properties:
        name:
          type: string
          nullable: true
        root_path:
          type: string
          nullable: true
        git_remote_url:
          type: string
          nullable: true
        git_provider:
          type: string
          nullable: true
        privacy_mode:
          type: string
          enum: [encrypted_git, plain]
          nullable: true
        project_key_id:
          type: string
          nullable: true
        updated_at:
          type: integer
          format: int64
    ProjectUpdateResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [project_id]
          properties:
            project_id:
              type: string
    ProjectGitTestRemoteRequest:
      type: object
      properties:
        remote_url:
          type: string
          nullable: true
          description: Optional remote URL override. If provided, project git_remote_url is updated.
        branch:
          type: string
          nullable: true
          description: Optional branch hint. If omitted, server resolves local default branch from HEAD/init.defaultBranch.
    ProjectGitTestRemoteResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [project_id, branch, status, remote_has_head]
          properties:
            project_id:
              type: string
            remote_url:
              type: string
              nullable: true
            branch:
              type: string
            status:
              type: string
              enum:
                [reachable, auth_failed, not_found, network_error, invalid_remote_url, remote_unset, unknown_error]
            remote_has_head:
              type: boolean
            error_code:
              type: string
              nullable: true
            error_message:
              type: string
              nullable: true
    ProjectGitPushRequest:
      type: object
      properties:
        branch:
          type: string
          nullable: true
          description: Optional branch to push. If omitted, server resolves local default branch from HEAD/init.defaultBranch.
        set_upstream:
          type: boolean
          nullable: true
          description: Optional; defaults to true.
    ProjectGitPushResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [project_id, branch, status, ahead_count, behind_count]
          properties:
            project_id:
              type: string
            remote_url:
              type: string
              nullable: true
            branch:
              type: string
            status:
              type: string
              enum:
                [pushed, up_to_date, auth_failed, not_found, network_error, non_fast_forward, remote_unset, unknown_error]
            ahead_count:
              type: integer
            behind_count:
              type: integer
            error_code:
              type: string
              nullable: true
            error_message:
              type: string
              nullable: true
            next_action:
              type: string
              nullable: true
    ProjectDeleteResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [project_id]
          properties:
            project_id:
              type: string
    ProjectRecoveryTokenExportRequest:
      type: object
      required: [pin]
      properties:
        pin:
          type: string
          description: User-provided PIN used to wrap the project key.
    ProjectRecoveryTokenExportResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [project_id, key_id, recovery_token]
          properties:
            project_id:
              type: string
            key_id:
              type: string
            recovery_token:
              type: string
              description: Serialized `.hrk` recovery token JSON payload.
    ProjectRecoveryTokenImportRequest:
      type: object
      required: [pin, recovery_token]
      properties:
        pin:
          type: string
        recovery_token:
          type: string
          description: Serialized `.hrk` recovery token JSON payload.
    ProjectRecoveryTokenImportResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [project_id]
          properties:
            project_id:
              type: string
    RecoveryTokenImportAutoResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required:
            [project_id, project_created, remote_hint_present, remote_configured, pull_status]
          properties:
            project_id:
              type: string
            project_created:
              type: boolean
            remote_hint_present:
              type: boolean
            remote_configured:
              type: boolean
            remote_error:
              type: string
              nullable: true
            pull_status:
              type: string
              enum: [not_attempted, succeeded, failed]
            pull_error:
              type: string
              nullable: true
    ProjectEncryptionCheck:
      type: object
      required: [ok, checked_files, unsafe_files, unsafe_paths, message]
      properties:
        ok:
          type: boolean
        checked_files:
          type: integer
        unsafe_files:
          type: integer
        unsafe_paths:
          type: array
          items:
            type: string
        message:
          type: string
    ProjectEncryptionCheckResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [project_id, privacy_mode, check]
          properties:
            project_id:
              type: string
            privacy_mode:
              type: string
              enum: [encrypted_git, plain]
            check:
              $ref: "#/components/schemas/ProjectEncryptionCheck"
    ResourceMetadata:
      type: object
      additionalProperties:
        type: array
        items:
          type: string
      description: Permissive metadata map. Repeated and custom values are preserved in order.
    AssetPlacement:
      type: object
      required: [placement_id, location_id, encoding, stored_byte_size, created_at]
      properties:
        placement_id:
          type: string
        location_id:
          type: string
        encoding:
          type: string
          enum: [plain, holder_asset_v1]
        stored_byte_size:
          type: integer
          format: int64
        created_at:
          type: integer
          format: int64
    ResourceAsset:
      type: object
      required: [asset_id, resource_id, original_filename, media_type, byte_size, plaintext_sha256, placements, created_at, updated_at]
      properties:
        asset_id:
          type: string
        resource_id:
          type: string
        original_filename:
          type: string
        media_type:
          type: string
        byte_size:
          type: integer
          format: int64
        plaintext_sha256:
          type: string
        placements:
          type: array
          items:
            $ref: "#/components/schemas/AssetPlacement"
        created_at:
          type: integer
          format: int64
        updated_at:
          type: integer
          format: int64
    Resource:
      type: object
      required: [resource_id, project_id, type, label, metadata, assets, created_at, updated_at]
      properties:
        resource_id:
          type: string
        project_id:
          type: string
        type:
          type: string
          description: Friendly, extensible type rather than a closed enum.
        label:
          type: string
        metadata:
          $ref: "#/components/schemas/ResourceMetadata"
        assets:
          type: array
          items:
            $ref: "#/components/schemas/ResourceAsset"
        created_at:
          type: integer
          format: int64
        updated_at:
          type: integer
          format: int64
    ResourceCreateRequest:
      type: object
      required: [project_id, type, label]
      properties:
        resource_id:
          type: string
          description: Optional; server generates if omitted.
        project_id:
          type: string
        type:
          type: string
        label:
          type: string
        metadata:
          $ref: "#/components/schemas/ResourceMetadata"
        created_at:
          type: integer
          format: int64
        updated_at:
          type: integer
          format: int64
    ResourceUpdateRequest:
      type: object
      properties:
        type:
          type: string
        label:
          type: string
        metadata:
          $ref: "#/components/schemas/ResourceMetadata"
          description: Partial metadata update. Empty arrays remove a property; omitted properties are preserved.
        updated_at:
          type: integer
          format: int64
    ResourceResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/Resource"
    ResourceListResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: array
          items:
            $ref: "#/components/schemas/Resource"
    ResourceDeleteResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [resource_id]
          properties:
            resource_id:
              type: string
    StorageLocation:
      type: object
      required: [location_id, project_id, name, provider, configuration, bound, created_at, updated_at]
      properties:
        location_id:
          type: string
        project_id:
          type: string
        name:
          type: string
        provider:
          type: string
          enum: [local_directory, s3_compatible]
        configuration:
          type: object
          additionalProperties:
            type: string
          description: Portable non-secret provider configuration.
        bound:
          type: boolean
        binding_preview:
          type: string
          nullable: true
          description: Safe display text; credentials are never returned.
        created_at:
          type: integer
          format: int64
        updated_at:
          type: integer
          format: int64
    StorageLocationRequest:
      type: object
      required: [project_id, name, provider]
      properties:
        location_id:
          type: string
        project_id:
          type: string
        name:
          type: string
        provider:
          type: string
          enum: [local_directory, s3_compatible]
        configuration:
          type: object
          additionalProperties:
            type: string
    StorageLocationListResponse:
      type: object
      required: [ok, data, preferred_location_id]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: array
          items:
            $ref: "#/components/schemas/StorageLocation"
        preferred_location_id:
          type: string
          nullable: true
    LocationBindingRequest:
      type: object
      required: [values]
      properties:
        values:
          type: object
          additionalProperties:
            type: string
          writeOnly: true
        preview:
          type: string
    AssetImportRequest:
      type: object
      required: [project_id, card_id, location_id, source_path]
      properties:
        project_id:
          type: string
        card_id:
          type: string
        location_id:
          type: string
        source_path:
          type: string
          description: Local readable regular file; never persisted into project metadata.
    AssetImportJob:
      type: object
      required: [job_id, status, duplicate_reused]
      properties:
        job_id:
          type: string
        status:
          type: string
          enum: [queued, staging, storing, committing, completed, failed]
        resource_id:
          type: string
          nullable: true
        asset_id:
          type: string
          nullable: true
        duplicate_reused:
          type: boolean
        error:
          type: string
          nullable: true
    AssetImportJobResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/AssetImportJob"
    TrashItem:
      type: object
      required: [type]
      properties:
        type:
          type: string
        card_id:
          type: string
          nullable: true
        message_id:
          type: string
          nullable: true
        thread_id:
          type: string
          nullable: true
        project_id:
          type: string
        title:
          type: string
          nullable: true
        role:
          type: string
          nullable: true
        deleted_at:
          type: integer
          format: int64
        created_at:
          type: integer
          format: int64
        rel_path:
          type: string
          nullable: true
    TrashListResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: array
          items:
            $ref: "#/components/schemas/TrashItem"
    TrashDeleteResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
    SearchCardsResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: array
          items:
            $ref: "#/components/schemas/SearchCardItem"
    SearchMessagesResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: array
          items:
            $ref: "#/components/schemas/SearchMessageItem"
    CardHistoryAuthor:
      type: object
      required: [name, email]
      properties:
        name:
          type: string
        email:
          type: string
    CardHistoryEntry:
      type: object
      required: [first_oid, last_oid, parent_oids, author, started_at, ended_at, kind, summary, commit_count, is_merge]
      properties:
        first_oid:
          type: string
        last_oid:
          type: string
        parent_oids:
          type: array
          items:
            type: string
        author:
          $ref: "#/components/schemas/CardHistoryAuthor"
        started_at:
          type: integer
          format: int64
        ended_at:
          type: integer
          format: int64
        kind:
          type: string
          enum: [created, updated, moved, links, milestones, deleted, restored, permanently_deleted, merged]
        summary:
          type: string
        commit_count:
          type: integer
          minimum: 1
        is_merge:
          type: boolean
    CardHistoryPageData:
      type: object
      required: [head_oid, entries, next_cursor]
      properties:
        head_oid:
          type: string
          nullable: true
        entries:
          type: array
          items:
            $ref: "#/components/schemas/CardHistoryEntry"
        next_cursor:
          type: string
          nullable: true
    CardHistoryPageResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/CardHistoryPageData"
    CardHistoryVersion:
      type: object
      required: [exists, oid, title, body]
      properties:
        exists:
          type: boolean
        oid:
          type: string
        title:
          type: string
        body:
          type: string
    CardHistoryDiffLine:
      type: object
      required: [origin, text, old_line, new_line]
      properties:
        origin:
          type: string
          enum: [" ", "+", "-"]
        text:
          type: string
        old_line:
          type: integer
          format: int64
          nullable: true
        new_line:
          type: integer
          format: int64
          nullable: true
    CardHistoryComparisonData:
      type: object
      required: [from, to, summary, lines, truncated]
      properties:
        from:
          $ref: "#/components/schemas/CardHistoryVersion"
        to:
          $ref: "#/components/schemas/CardHistoryVersion"
        summary:
          type: string
        lines:
          type: array
          items:
            $ref: "#/components/schemas/CardHistoryDiffLine"
        truncated:
          type: boolean
    CardHistoryComparisonResponse:
      type: object
      required: [ok, data]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          $ref: "#/components/schemas/CardHistoryComparisonData"
paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /openapi.yaml:
    get:
      summary: Download the OpenAPI specification
      security: []
      responses:
        "200":
          description: OK
          content:
            application/yaml:
              schema:
                type: string
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai_catalog.yaml:
    get:
      summary: Download canonical AI catalog config (models + cloud)
      security: []
      responses:
        "200":
          description: OK
          content:
            application/yaml:
              schema:
                type: string
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai_catalog.json:
    get:
      summary: Download canonical AI catalog config as JSON
      security: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /git_providers.yaml:
    get:
      summary: Download git provider UI/runtime config
      security: []
      responses:
        "200":
          description: OK
          content:
            application/yaml:
              schema:
                type: string
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /git_providers.json:
    get:
      summary: Download git provider UI/runtime config as JSON
      security: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /rebuild:
    post:
      summary: Rebuild database state from project files
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RebuildRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RebuildResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Project not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /reindex:
    post:
      summary: Rebuild search indexes from database state
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReindexResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /projects:
    get:
      summary: List projects
      parameters:
        - in: query
          name: name
          required: false
          schema:
            type: string
          description: Substring match on project name.
        - in: query
          name: updated_after
          required: false
          schema:
            type: integer
            format: int64
          description: Return projects with updated_at >= updated_after.
        - in: query
          name: updated_before
          required: false
          schema:
            type: integer
            format: int64
          description: Return projects with updated_at <= updated_before.
        - in: query
          name: order
          required: false
          schema:
            type: string
            enum:
              [updated_at_desc, updated_at_asc, created_at_desc, created_at_asc, name_desc, name_asc]
          description: Order by updated_at (default), created_at, or name.
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            format: int32
            minimum: 0
            maximum: 1000
          description: Max results (default 100, capped at 1000).
        - in: query
          name: offset
          required: false
          schema:
            type: integer
            format: int32
            minimum: 0
          description: Offset into ordered results (default 0).
        - in: query
          name: count
          required: false
          schema:
            type: boolean
            default: false
          description: Include count fields (card_count/root_card_count) per project.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectListResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create a project
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectCreateRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectCreateResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /projects/{project_id}:
    get:
      summary: Get a project
      parameters:
        - in: path
          name: project_id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectGetResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      summary: Update a project
      parameters:
        - in: path
          name: project_id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectUpdateRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectUpdateResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete a project
      parameters:
        - in: path
          name: project_id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectDeleteResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /projects/{project_id}/history/cards/{card_id}:
    get:
      summary: List saved history for a card
      description: Returns commits reachable from the captured project HEAD that changed the card. Adjacent autosaves may be grouped into one entry.
      parameters:
        - in: path
          name: project_id
          required: true
          schema:
            type: string
        - in: path
          name: card_id
          required: true
          schema:
            type: string
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - in: query
          name: cursor
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Saved card history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardHistoryPageResponse"
        "400":
          description: Invalid card identifier, limit, or cursor
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Project not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The encrypted project key is unavailable on this device
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Project history is temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /projects/{project_id}/history/cards/{card_id}/compare:
    get:
      summary: Compare two saved versions of a card
      parameters:
        - in: path
          name: project_id
          required: true
          schema:
            type: string
        - in: path
          name: card_id
          required: true
          schema:
            type: string
        - in: query
          name: from
          required: true
          schema:
            type: string
        - in: query
          name: to
          required: true
          schema:
            type: string
        - in: query
          name: mode
          required: false
          schema:
            type: string
            enum: [since]
            default: since
      responses:
        "200":
          description: A line-oriented card comparison
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardHistoryComparisonResponse"
        "400":
          description: Invalid or missing comparison parameters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Project not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The encrypted project key is unavailable on this device
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Project history is temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /projects/{project_id}/git/test-remote:
    post:
      summary: Test project remote reachability/auth
      parameters:
        - in: path
          name: project_id
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectGitTestRemoteRequest"
      responses:
        "200":
          description: Probe completed with structured status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectGitTestRemoteResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /projects/{project_id}/git/push:
    post:
      summary: Push project repository to remote
      parameters:
        - in: path
          name: project_id
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectGitPushRequest"
      responses:
        "200":
          description: Push attempted with structured status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectGitPushResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /projects/{project_id}/recovery-token/export:
    post:
      summary: Export project recovery token
      parameters:
        - in: path
          name: project_id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectRecoveryTokenExportRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectRecoveryTokenExportResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /projects/{project_id}/recovery-token/import:
    post:
      summary: Import project recovery token
      parameters:
        - in: path
          name: project_id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectRecoveryTokenImportRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectRecoveryTokenImportResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /recovery-token/import:
    post:
      summary: Import recovery token and auto-match/create project
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProjectRecoveryTokenImportRequest"
      responses:
        "200":
          description: Imported and matched an existing local project_id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecoveryTokenImportAutoResponse"
        "201":
          description: Imported and created a new local project_id from token metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecoveryTokenImportAutoResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Keyring unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /projects/{project_id}/encryption-check:
    get:
      summary: Check encrypted project card blobs before push/sync
      parameters:
        - in: path
          name: project_id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectEncryptionCheckResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /calendar:
    get:
      summary: List project milestones and card activity in a time range
      parameters:
        - in: query
          name: project_id
          required: true
          schema:
            type: string
        - in: query
          name: from
          required: true
          schema:
            type: integer
            format: int64
        - in: query
          name: to
          required: true
          schema:
            type: integer
            format: int64
      responses:
        "200":
          description: Calendar activity in the requested inclusive epoch-second range.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectCalendarResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Project not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /cards/{card_id}/milestones:
    get:
      summary: List milestones attached to a card
      parameters:
        - in: path
          name: card_id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MilestoneListResponse"
        "404":
          description: Card not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Add a milestone to a card
      parameters:
        - in: path
          name: card_id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MilestoneCreateRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MilestoneResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Card not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /cards/{card_id}/milestones/{milestone_id}:
    delete:
      summary: Remove a milestone from a card
      parameters:
        - in: path
          name: card_id
          required: true
          schema:
            type: string
        - in: path
          name: milestone_id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Removal result
          content:
            application/json:
              schema:
                type: object
        "404":
          description: Card not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /cards:
    get:
      summary: List cards
      parameters:
        - in: query
          name: project_id
          required: true
          schema:
            type: string
        - in: query
          name: view
          required: false
          schema:
            type: string
            enum: [tree, recent]
            default: tree
          description: Card list mode. tree returns one hierarchy level (roots or a parent's children); recent returns recently updated cards.
        - in: query
          name: parent_card_id
          required: false
          schema:
            type: string
          description: Optional when view=tree. Omit for roots; set to return that parent card's children.
        - in: query
          name: order
          required: false
          schema:
            type: string
            enum: [tree_default, updated_desc, title_asc]
          description: |
            Card ordering strategy. Defaults are view-aware: tree uses tree_default
            (sort_key asc, updated_at desc, title asc), recent uses updated_desc.
        - in: query
          name: count
          required: false
          schema:
            type: boolean
            default: false
          description: Include count fields (child_count) in returned card items.
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 5000
            default: 200
          description: Maximum number of cards when view=recent.
        - in: query
          name: include_deleted
          required: false
          schema:
            type: integer
          description: Set to 1 to include deleted cards.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardListResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create a card
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardCreateRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardCreateResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /cards/context:
    get:
      summary: Read card context and breadcrumb path for a project level
      parameters:
        - in: query
          name: project_id
          required: true
          schema:
            type: string
        - in: query
          name: parent_card_id
          required: false
          schema:
            type: string
          description: Optional parent card id. Omit for project root level.
        - in: query
          name: order
          required: false
          schema:
            type: string
            enum: [tree_default, updated_desc, title_asc]
            default: tree_default
          description: Ordering for the returned level cards.
        - in: query
          name: count
          required: false
          schema:
            type: boolean
            default: false
          description: Include count fields (child_count) in context cards.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardContextResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Project or parent card not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /cards/{card_id}:
    get:
      summary: Fetch a card
      parameters:
        - name: card_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardGetResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /cards/{card_id}/move:
    post:
      summary: Move/reparent/reorder a card using card move intent
      parameters:
        - name: card_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardMoveRequest"
      responses:
        "200":
          description: Move applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardMoveResponse"
        "400":
          description: Bad request (missing/invalid intent fields)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Card or target not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Stale revision (if_revision mismatch)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Invalid move (cycle/cross-project/invalid target)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /cards/{card_id}/links:
    get:
      summary: List outgoing links for a card
      parameters:
        - name: card_id
          in: path
          required: true
          schema:
            type: string
        - name: include_deleted
          in: query
          required: false
          schema:
            type: integer
            enum: [0, 1]
          description: Include links to deleted targets.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardLinkListResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create or update a link from a card
      parameters:
        - name: card_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardLinkCreateRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardLinkCreateResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete links from a card
      description: Provide to_card_id (and optional kind) to delete a specific link. If omitted, deletes all outgoing links.
      parameters:
        - name: card_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardLinkDeleteRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardPatchResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /cards/{card_id}/backlinks:
    get:
      summary: List backlinks to a card
      parameters:
        - name: card_id
          in: path
          required: true
          schema:
            type: string
        - name: include_deleted
          in: query
          required: false
          schema:
            type: integer
            enum: [0, 1]
          description: Include backlinks from deleted sources.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardLinkListResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/capabilities:
    get:
      summary: Get local model runner capabilities
      parameters:
        - in: query
          name: project_id
          required: false
          schema:
            type: string
          description: Optional project id for project-scoped router config in response.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunnerCapabilitiesResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/status:
    get:
      summary: Get live AI runtime status
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiStatusResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/providers/credentials:
    get:
      summary: List configured cloud provider credentials
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiProviderCredentialsListResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Create or update a cloud provider API key
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiProviderCredentialUpsertRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiProviderCredentialUpsertResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/providers/catalog:
    get:
      summary: Get provider-agnostic cloud catalog with configured flags
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiProviderCatalogResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/providers/credentials/{provider}:
    delete:
      summary: Delete a configured cloud provider API key
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiProviderCredentialDeleteResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/runner/retry:
    post:
      summary: Retry local model runner probe
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunnerCapabilitiesResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "501":
          description: Not implemented
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/local-models/config:
    get:
      summary: Get local model configuration
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiLocalModelConfigResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Set or clear local model configuration
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiLocalModelConfigUpdateRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiLocalModelConfigResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/runs:
    post:
      summary: Create and execute an AI run with routing and streaming
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiRunCreateRequest"
      responses:
        "200":
          description: OK
          content:
            text/event-stream:
              schema:
                type: string
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: No local runner or configured cloud provider available
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    get:
      summary: List AI runs by project or thread
      parameters:
        - in: query
          name: project_id
          required: false
          schema:
            type: string
        - in: query
          name: thread_id
          required: false
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiRunListResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/runs/{run_id}:
    get:
      summary: Fetch an AI run
      parameters:
        - in: path
          name: run_id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiRunGetResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/runs/{run_id}/events:
    get:
      summary: Stream run events (SSE)
      parameters:
        - in: path
          name: run_id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            text/event-stream:
              schema:
                type: string
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/runner/pull:
    post:
      summary: Start pulling a local model
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [model]
              properties:
                model:
                  type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunnerPullStartResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "501":
          description: Not implemented
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Runner unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/runner/pull/{job_id}:
    get:
      summary: Fetch pull job status
      parameters:
        - in: path
          name: job_id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RunnerPullStatusResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "501":
          description: Not implemented
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/runner/pull/{job_id}/events:
    get:
      summary: Stream pull job progress (SSE)
      parameters:
        - in: path
          name: job_id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            text/event-stream:
              schema:
                type: string
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "501":
          description: Not implemented
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/messages:
    get:
      summary: List AI messages by thread
      parameters:
        - in: query
          name: thread_id
          required: true
          schema:
            type: string
        - in: query
          name: include_deleted
          required: false
          schema:
            type: integer
          description: Set to 1 to include deleted messages.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiMessageListResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create an AI message
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiMessageCreateRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiMessageCreateResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/messages/capture:
    post:
      summary: Capture prompt/response pair with provenance
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiMessageCaptureRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiMessageCaptureResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/threads:
    get:
      summary: List AI threads by project
      parameters:
        - in: query
          name: project_id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiThreadListResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create an AI thread
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiThreadCreateRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiThreadCreateResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/threads/{thread_id}:
    get:
      summary: Fetch an AI thread
      parameters:
        - name: thread_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiThreadGetResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      summary: Update AI thread metadata
      parameters:
        - name: thread_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiThreadUpdateRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiThreadPatchResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete an AI thread
      parameters:
        - name: thread_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiThreadPatchResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /resources:
    get:
      summary: List resources by project
      parameters:
        - in: query
          name: project_id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceListResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create a resource
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResourceCreateRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Conflict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /resources/{resource_id}:
    get:
      summary: Get a resource with its assets and placements
      parameters:
        - name: resource_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      summary: Update a resource
      parameters:
        - name: resource_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResourceUpdateRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete a resource and remove its Card links
      parameters:
        - name: resource_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceDeleteResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /resources/{resource_id}/assets/{asset_id}/content:
    get:
      summary: Stream verified asset content
      parameters:
        - name: resource_id
          in: path
          required: true
          schema:
            type: string
        - name: asset_id
          in: path
          required: true
          schema:
            type: string
        - name: placement_id
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Recovered plaintext asset
          headers:
            Content-Disposition:
              schema:
                type: string
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "400":
          description: Missing binding, key, storage, or integrity failure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Resource, asset, or placement not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /locations:
    get:
      summary: List project storage locations
      parameters:
        - name: project_id
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageLocationListResponse"
    post:
      summary: Create a storage location declaration
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StorageLocationRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                type: object
  /locations/{location_id}:
    get:
      summary: Get a storage location
      parameters:
        - &locationId
          name: location_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
    patch:
      summary: Update a storage location declaration
      parameters:
        - *locationId
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                configuration:
                  type: object
                  additionalProperties:
                    type: string
      responses:
        "200":
          description: Updated
    delete:
      summary: Delete an unused storage location declaration
      parameters:
        - *locationId
      responses:
        "200":
          description: Deleted
        "409":
          description: A live placement still uses the location
  /locations/{location_id}/binding:
    put:
      summary: Bind private device credentials or path
      parameters:
        - *locationId
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LocationBindingRequest"
      responses:
        "200":
          description: Bound; secret values are not returned
    delete:
      summary: Remove this device's private binding
      parameters:
        - *locationId
      responses:
        "200":
          description: Unbound
  /locations/{location_id}/test:
    post:
      summary: Upload, verify, and remove a storage probe
      parameters:
        - *locationId
      responses:
        "200":
          description: Location available
        "400":
          description: Missing binding or storage failure
  /locations/preferred:
    put:
      summary: Set this device's preferred project storage location
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [project_id, location_id]
              properties:
                project_id:
                  type: string
                location_id:
                  type: string
      responses:
        "200":
          description: Preferred location saved in the device keyring
  /imports:
    post:
      summary: Start a local-file asset import
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetImportRequest"
      responses:
        "202":
          description: Import queued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetImportJobResponse"
  /imports/{job_id}:
    get:
      summary: Poll an asset import job
      parameters:
        - name: job_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Current job state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetImportJobResponse"
        "404":
          description: Job not found
  /trash:
    get:
      summary: List trashed items by project
      parameters:
        - in: query
          name: project_id
          required: true
          schema:
            type: string
        - in: query
          name: type
          required: false
          schema:
            type: string
          description: card | ai_message | all
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrashListResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Empty trash by project
      parameters:
        - in: query
          name: project_id
          required: true
          schema:
            type: string
        - in: query
          name: type
          required: false
          schema:
            type: string
          description: card | ai_message | all
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrashDeleteResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /trash/{type}/{id}:
    delete:
      summary: Permanently delete a trashed item
      parameters:
        - name: type
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TrashDeleteResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/messages/{message_id}:
    get:
      summary: Fetch an AI message
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiMessageGetResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      summary: Update an AI message
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AiMessageUpdateRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiMessageUpdateResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Move an AI message to trash
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiMessageUpdateResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/messages/{message_id}/restore:
    post:
      summary: Restore an AI message from trash
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiMessageUpdateResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/messages/{message_id}/links:
    get:
      summary: List outgoing links for an AI message
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
        - name: include_deleted
          in: query
          required: false
          schema:
            type: integer
            enum: [0, 1]
          description: Include links to deleted targets.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardLinkListResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create or update a link from an AI message
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardLinkCreateRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardLinkCreateResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete links from an AI message
      description: Provide to_card_id (and optional to_type/kind) to delete a specific link. If omitted, deletes all outgoing links.
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardLinkDeleteRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiMessageCreateResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /ai/messages/{message_id}/backlinks:
    get:
      summary: List backlinks to an AI message
      parameters:
        - name: message_id
          in: path
          required: true
          schema:
            type: string
        - name: include_deleted
          in: query
          required: false
          schema:
            type: integer
            enum: [0, 1]
          description: Include backlinks from deleted sources.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardLinkListResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      summary: Update card content and/or title
      parameters:
        - name: card_id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CardUpdateRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardPatchResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Move card to trash
      parameters:
        - name: card_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardPatchResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /cards/{card_id}/restore:
    post:
      summary: Restore a card from trash
      parameters:
        - name: card_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CardPatchResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /search/cards:
    get:
      summary: Search cards
      parameters:
        - name: project_id
          in: query
          required: true
          schema:
            type: string
        - name: q
          in: query
          required: true
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
        - name: offset
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SearchCardsResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /search/ai:
    get:
      summary: Search AI messages
      parameters:
        - name: project_id
          in: query
          required: true
          schema:
            type: string
        - name: q
          in: query
          required: true
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
        - name: offset
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SearchMessagesResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
